const WEEKDAY = 'weekday';
const MONTH = 'month';
const DAY = 'day';
const YEAR = 'year';
const HOUR = 'hour';
const MINUTES = 'minute';
const SECONDS = 'second';
const MILLISECONDS = 'millisecond';

module.exports = {
    WEEKDAY,
    MONTH,
    DAY,
    YEAR,
    HOUR,
    MINUTES,
    SECONDS,
    MILLISECONDS
};
